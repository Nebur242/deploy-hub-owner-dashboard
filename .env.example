# OpenAI API Key for workflow generation
OPENAI_API_KEY=your_openai_api_key_here

# Paddle Configuration
# Note: Paddle.js is loaded dynamically, no publishable key needed on frontend
# The client token is provided by the API during checkout

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Firebase Configuration (for admin SDK)
FIREBASE_SERVICE_ACCOUNT_KEY='{}'

# Add other environment variables as needed
# NEXTAUTH_SECRET=your_nextauth_secret
# NEXTAUTH_URL=http://localhost:3000
